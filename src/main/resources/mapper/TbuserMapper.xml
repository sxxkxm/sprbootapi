<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.template.thcspringboot.data.mapper.TbuserMapper">
	
<select id="findById" parameterType="String" resultType="Tbuser">
	SELECT * 
	  FROM tbuser
	 WHERE id = #{tbuserID}
</select>

</mapper>